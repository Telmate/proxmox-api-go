# `create guest qemu` or `createQemu` (legacy) JSON Sample

```json
{
  "full": {
    "node": "pve-latest",
    "name": "golang2.test.com",
    "pool": "my-pool",
    "storage": "local-zfs",
    "format": "raw"
  }
}
```
